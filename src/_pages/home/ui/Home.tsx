import { MainIntro } from '@/widgets/main-intro';
import { Navigation } from '@/widgets/main-navigation';

export const Home = () => {
  return (
    <div>
      <Navigation />
      <MainIntro />
    </div>
  );
};
